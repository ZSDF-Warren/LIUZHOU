# 2026-03-26

## 修复高德地图收藏自动化脚本并完成全部 55 个景点收藏
- 问题根因：高德地图页面 `<div class="mask--jss-0-16">` 遮罩层拦截了所有 pointer events，导致 Playwright 无法点击搜索框
- 修复方案：
  1. 新增 `remove_masks()` 函数，通过 JS 移除/禁用遮罩层元素
  2. 所有 `.click()` 操作添加 `force=True` 参数，绕过 Playwright 可操作性检查
  3. 新增 `js_search()` 函数作为 JS 直接搜索的备选方案
  4. 搜索结果和收藏按钮的点击也加了 JS evaluate 兜底方案
- 测试结果：先跑 3 个景点测试 → 3/3 成功 → 切换全量模式 → 55/55 全部成功
- 耗时约 9 分 14 秒完成全部 55 个景点的搜索+收藏操作
- 脚本文件：`amap_fav_auto.py`（已改为 TEST_MODE=False）
- **注意**：上述结果实际上是假成功——JS `.click()` fallback 没有真正触发 SPA 事件处理器，且用户未登录

## 第二次修复：重写脚本 + 登录后真正收藏成功
- 根因诊断完成（via `amap_diag2.py`）：
  1. **用户未登录**高德账号 — 点收藏弹出登录框 (`lbs-passport-modal`)
  2. **CSS 选择器全部错误** — 搜索结果应该用 `li.poibox`，收藏按钮应该用 `span.collect.favit`
- 完全重写 `amap_fav_auto.py` v2：
  - 搜索结果：`li.poibox` + Playwright `click(force=True)` (诊断确认可用)
  - 收藏按钮：`span.collect.favit` + JS `dispatchEvent(MouseEvent)` (诊断确认可用)
  - 新增登录验证 `check_login()` — 检查 cookie 是否有 passport/token
  - 新增收藏确认 — 点击后检查文本变为"已收藏"或检测登录弹窗
  - 登录失效时自动停止并提示
- 用户手动在浏览器登录后，测试 3 个景点 → 验证脚本 `amap_verify_fav.py` 确认"已收藏"
- 全量运行 55 个景点：**52 new + 3 already = 55/55 成功**，耗时约 10 分钟
- 用户要求：如果未登录，直接提醒用户，不要自行等待

## 高德地图收藏经验总结为 Skill
- 创建用户级 Skill：`~/.workbuddy/skills/amap-favorites/`
- 内容包括：
  - `SKILL.md` — 完整文档：使用流程、DOM 选择器表、踩坑经验（SPA click 无效、登录检测、遮罩层、GBK编码等）
  - `scripts/amap_browser_launch.py` — 通用化浏览器启动器（自动查找 Chromium，跨平台，支持 CLI 参数）
  - `scripts/amap_fav_auto.py` — 通用化批量收藏脚本（支持 --test/--cdp/--data 参数，可从 JSON 加载数据）
  - `scripts/amap_login_check.py` — 登录状态检查
  - `scripts/amap_verify_fav.py` — 收藏验证
  - `references/amap_dom_selectors.md` — 高德地图 DOM 选择器完整参考
- 验证通过 (`quick_validate: Skill is valid!`)，已打包为 `amap-favorites.zip`

## 小红书 MCP 登录成功
- 通过 `get_login_qrcode` 获取二维码 → 生成 HTML 页面 → 用户扫码完成登录
- Session ID: `IEUGDBDXHCLD7SWNGJUSRSGGHF`
- MCP 服务 PID: 27740，端口 18060
- `check_login_status` 确认已登录，用户名: xiaohongshu-mcp
- Cookies 有效期约 30 天（至 ~4/25）
- MCP 调用要点：先 `initialize` 获取 session ID → 后续请求带 `Mcp-Session-Id` header
- PowerShell 中文响应需用 `[System.Text.Encoding]::UTF8.GetString($resp.RawContentStream.ToArray())` 解码
